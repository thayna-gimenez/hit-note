# 🛠 Quickstart

Na pasta principal do projeto faça:

```
docker compose up --build
```

Agora, execute:

```
cd frontend
npm run build
serve -s build
```

Com todos esses comandos finalizados, acesse pelo seu navegador (Chrome/Edge):
http://localhost:3000 

No canto superior direito do endereço, instale o aplicativo desktop Hit.note.
